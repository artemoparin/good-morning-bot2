name: Send Telegram Message

on:
  schedule:
    # Запускать каждый день в 07:00 UTC (доброе утро)
    - cron: '0 7 * * *'
    # Запускать каждый день в 23:00 UTC (спокойной ночи)
    - cron: '0 23 * * *'
  workflow_dispatch: # Позволяет запускать скрипт вручную из интерфейса GitHub

jobs:
  send-message:
    runs-on: ubuntu-latest
    steps:
    - name: Send test messages
      run: |
        # Всегда отправляем оба сообщения при ручном запуске
        echo "Отправляем сообщения..."
        
        # Отправляем утреннее сообщение
        curl -s -X POST https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/sendMessage \
          -d chat_id=${{ secrets.CHAT_ID }} \
          -d text="Доброе утро, любимая! ☀️ Пусть твой день будет наполнен улыбками и радостью! 💖"
        
        echo "Утреннее сообщение отправлено"
        
        # Ждем 2 секунды
        sleep 2
        
        # Отправляем ночное сообщение
        curl -s -X POST https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/sendMessage \
          -d chat_id=${{ secrets.CHAT_ID }} \
          -d text="Спокойной ночи, моя прекрасная! 💫 Пусть тебе снятся самые сладкие сны. 💕"
        
        echo "Ночное сообщение отправлено"
